class ball{
  float x, y, radius, vx, vy, ax, ay;
  int fill_color;
  public ball(float x, float, y, float radius, float vx, float vy, float, ax, float, ay){
    this.x = x;
    this.y = y;
    this.radius = radius;
    this.vx = vx;
    this.vy = vy;
    this.fill_color
    this.ax = ax;
    this.ay = ay;
  }
}
